<h1>Campgrounds Page</h1>


<% campgrounds.forEach(function(camp){ %>
<div>
        <h4><%= camp.name %></h4>
        <img src="<%= camp.image %>" alt="Picture of <%= camp.name %>">
</div>

<% }); %>
